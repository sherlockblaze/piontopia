package utils

/*
UpdateParameter replace the target's attribute if it's different with source's while they both have it
@param target
@param source
**/
func UpdateParameter(target, source interface{}) interface{} {
	return target
}
