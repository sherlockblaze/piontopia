syntax = "proto3";

package proto;

message Labels {
    string ID = 1;
    LabelKind Kind = 2;
    LabelLevel Level = 3;
    repeated string Labels = 4;
}

enum LabelKind {
    OWN = 0;
    OTHERS = 1;
}

enum LabelLevel {
    USER = 0;
    TEAM = 1;
}